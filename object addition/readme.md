Open the file object_addition(1).ipynb
Click on run in jupyter notebook or colab
Type the name of the object you want to add
Add the background image for the object
The code does its work by searching from all the availble sources and inserts it into image.
Next we can adjust the coordinates of it and increase or decrease the size and rotate it.